apply plugin: 'com.android.application'

android {
    signingConfigs {
        gdut {
            keyAlias 'gdutzt'
            keyPassword '011715'
            storeFile file('C:/Users/zhangtao/Desktop/Documents/签名打包/first20160426.txt')
            storePassword '011715'
        }
    }
    compileSdkVersion 23
    buildToolsVersion "23.0.2"
    defaultConfig {
        applicationId "com.example.zhangtao.clock"
        minSdkVersion 15
        targetSdkVersion 23
        versionCode 1
        versionName "1.0"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    testCompile 'junit:junit:4.12'
    compile 'com.android.support:appcompat-v7:23.2.0'
}
